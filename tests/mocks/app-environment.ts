/** Vitest stub for SvelteKit's virtual `$app/environment` module (aliased in vitest.config.ts). */
export const building = false;
export const dev = true;
export const browser = false;
export const version = 'test';
